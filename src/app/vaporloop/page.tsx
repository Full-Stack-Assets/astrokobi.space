import VaporLoop from './VaporLoop';

export const metadata = {
  title: 'VaporLoop',
  description: 'VaporLoop V3.3 — an autonomous app engine factory demo.',
  alternates: { canonical: '/vaporloop' },
};

export default function VaporLoopPage() {
  return <VaporLoop />;
}
